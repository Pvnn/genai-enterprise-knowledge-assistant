/**
 * Main chat interface.
 * Owner: P7
 *
 * Handles:
 *   - Sending user query via POST /chat (SSE)
 *   - Streaming token events into the message bubble
 *   - Rendering clarify / refusal / conflict final events
 *   - Displaying inline citations
 *   - Thumbs up/down feedback buttons (Priority 2)
 */
import React from "react";

// TODO P7: implement the full chat UI
const ChatPage: React.FC = () => {
  return <div>Chat – P7 to implement</div>;
};

export default ChatPage;
